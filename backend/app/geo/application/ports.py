"""
Порты (контракты) исходящих зависимостей geo, которыми пользуется application-слой.

По инверсии зависимостей контракт, на который опирается ``CityService``, принадлежит
application-слою, а ``infra`` его **реализует** (``infra`` импортирует порт отсюда, не
наоборот). На этот же порт опирается in-memory фейк в тестах — ``ty`` ловит расхождение
сигнатур между реальным репозиторием и фейком.

Зависит только от ``domain`` — модуль остаётся листом графа импортов без циклов.
"""

from typing import Protocol

from app.geo.domain.entities import City


class CityRepositoryPort(Protocol):
    """Контракт read-only поиска городов по газеттиру, на который опирается application-слой."""

    async def search(self, *, search_text: str, limit: int) -> list[City]:
        """
        Ищет города по ПРЕФИКСУ имени (en/ru), отсортированные по убыванию населения.

        Префиксный матч ``lower(name) LIKE 'q%'`` ложится на частичный btree
        ``text_pattern_ops`` — берётся range-scan'ом для запроса любой длины.

        Args:
            search_text: Текст для префиксного матча по имени. Репозиторий экранирует
                LIKE-метасимволы (``%`` ``_`` ``\\``) перед подстановкой в паттерн.
            limit: Максимум кандидатов в выдаче

        Returns:
            Список городов, отсортированный по убыванию населения
        """
        ...
