import { GlobeCanvas } from "./globe/GlobeCanvas";

/**
 * Декоративный глобус-герой дашборда: тёплый тонированный автовращающийся глобус —
 * спокойный якорь экрана и единый signature с auth/лендингом. Тонкая обёртка над
 * общим `GlobeCanvas`.
 *
 * Без дуг и подписей: на дашборде планета работает как спокойный фон под данными, а
 * не как карта маршрутов — рисовать на ней чужие (мок) перелёты значило бы выдавать
 * их за поездки пользователя. Маршруты остаются языком лендинга.
 */
export function HomeGlobe() {
  return <GlobeCanvas pov={{ lat: 25, lng: 20, altitude: 2.4 }} />;
}
