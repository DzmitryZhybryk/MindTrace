# VCS
.git
.gitignore
.gitattributes

# Python artifacts
__pycache__
*.py[cod]
*.egg-info
.pytest_cache
.ruff_cache
.ty_cache
.mypy_cache
.venv
venv

# Build context pollution (sent to daemon but never needed in image)
Dockerfile
.dockerignore
docker-compose*.yaml
docker-compose*.yml
Makefile
.env
.env.*
!.env.example

# IDE / tooling
.claude
.idea
.vscode
*.swp
*.swo

# Docs and repo-local notes (README.md stays — pyproject references it)
docs
CHANGELOG.md
CLAUDE.md
ZCloude
ZTODO

# Test artifacts
tests
.coverage
htmlcov

# Logging stack configs (mounted via compose volumes, not baked into the image)
loki.yaml
promtail-config.yml
